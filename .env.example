# OpenAI
OPENAI_API_KEY=sk-...

# Retrieval Neo4j (external — your existing database, NOT in Docker)
NEO4J_URI=bolt://host.docker.internal:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your-retrieval-db-password

# Memory Neo4j is auto-configured in docker-compose (no need to set here)
